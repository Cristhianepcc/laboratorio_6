# Carga las definiciones (colas del lab) al iniciar el broker
load_definitions = /etc/rabbitmq/definitions.json
# Permite que guest se conecte (por defecto guest solo funciona desde localhost;
# dentro del contenedor lo dejamos abierto para la HTTP API del lab)
loopback_users = none
